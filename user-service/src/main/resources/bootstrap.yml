# application.yml 보다 우선순위가 높은 설정 파일과 읽을 설정 파일 등록
spring:
  cloud:
    config:
      uri: http://localhost:8888  # Config-server 주소
      name: user-service  # config-server 의  yml 파일명
#  profiles:
#    active: dev